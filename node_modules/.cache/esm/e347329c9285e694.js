let express,bodyParser,cors,router;_928‍.w("express",[["default",["express"],function(v){express=v}]]);_928‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_928‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_928‍.w("./routes/index.js",[["router",["router"],function(v){router=v}]]);

 // for parsing the body of the request
 // for allowing cross-origin requests

const app = express(); // create an instance of express

app.use("/api", router); // use the router

// middleware

app.use(cors()); // enable cors

app.use(bodyParser.json()); // support json encoded bodies

app.use(express.urlencoded({ extended: true })); // support encoded bodies

const PORT = process.env.PORT || 5001;

app.listen(PORT, () => {
  console.log(`Server is listening on port ${PORT}`);
}); // start the server

app.get("/", (req, res) => {
  res.json({ message: "Hello World" });
});
