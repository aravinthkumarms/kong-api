let mockData;_1bb‍.x([["getService",()=>getService]]);_1bb‍.w("../mock",[["default",["mockData"],function(v){mockData=v}]]);

       const getService = (req, res) => {
  const { id } = req.params;
  const service = mockData.find((user) => user.userId === id);
  if (service) {
    res.send(service);
  } else {
    res.status(404).send("User not found");
  }
};
